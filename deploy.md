```text
[AGENT SETUP INSTRUCTIONS: act on everything from here until the [USER-FACING DEPLOYMENT] marker. Do not paste or read these agent-instruction lines to the deployer.]

## DONE WHEN

This deployment is complete only when all of the following are true. Treat these as the acceptance criteria for the whole task, not as aspirations:

- All eight surfaces exist in this workspace, named exactly: Context (database), Current Focus, Core Profile, Clyo Backlog, Capture Queue, Task Journal (database), Drift Log (database), and Open Clyo Version.
- The Context database has its four properties, the Task Journal database has its two (Name and Entry Date), and the Drift Log database has all seven. The Status, Severity, and Model selects carry the option sets specified in their steps; Topic is intentionally left open for the user to populate.
- The Core Profile page contains all six starter sections as real headings.
- The Open Clyo Version page exists and contains the single required version-stamp line with today's date.
- The deployer has pasted the full clyo-instructions.md into the Notion AI Instructions page and confirmed the closing line is present.
- Every placeholder in the instruction set is wired to a live surface, confirmed by the deployer's three-part Step 3 check: all nine wiring spots are confirmed as live surface mentions against the named list, a search for `URL]` returns zero, and a search for `[PLACEHOLDER:` returns exactly 2 (the two intentional readiness references).

Do not tell the deployer Clyo is deployed until all three Step 3 checks pass.

## HOW TO RUN THIS

- Each surface below is a numbered step (Surface 1 through Surface 8). Work them in order. Do not skip, merge, or reorder steps.
- Before creating any surface, search this workspace for a page or database with that exact name. This deploy assumes a fresh empty workspace, so no surface should already exist. If one does, stop and tell the deployer that this deploy assumes a fresh empty workspace, name the collision, and ask them to resolve it (use a clean workspace, or remove or rename the conflicting item) before continuing. If none exists, create it and confirm before moving to the next.
- After creating each surface, verify it exists and is named correctly before moving on. If a creation step looks like it failed, first check whether the surface was in fact created before retrying, so you do not duplicate it. If you cannot complete a step, stop and report which step failed and the current state of the workspace, rather than pressing on.
- Hold two cautions throughout, both detailed at their step: long instruction sets can silently truncate on paste (Step 1), and the instruction set contains two intentional placeholder-style references that must be left alone (Step 2).
- Two registers run through this prompt. Lines marked as agent setup instructions are for you to act on; the warm, user-facing narration described in each step is what you actually say to the deployer. Do not read the bracketed agent-instruction lines to them.
- The italic parenthetical at the start of each surface step, *(Check it does not already exist, create, then confirm before the next.)*, is a silent agent checklist, not deployer narration. Do not read it aloud. Act on it, then narrate the surface creation in your own words.

You are setting up Clyo in this Notion workspace. Clyo is a Notion-native personal AI assistant and advisor that runs inside Notion AI, a persistent, context-aware thinking partner that learns how you work and helps you stay grounded across projects and sessions. You are going to create the eight surfaces Clyo reads from and writes to, then guide the deployer through installing the instruction set that brings it to life.

[USER-FACING DEPLOYMENT: everything from here is narration and actions for the deployer. Do not read this marker line to them; begin directly with the introduction below.]

Introduce yourself warmly and explain what you are about to do together: you will build eight surfaces in this workspace, then walk them through installing Clyo's brain into Notion AI's instructions.
Tell them this takes about 15 to 20 minutes.
You will handle the creation steps and confirm each surface with them as you go.
Tell them there are a couple of things only they can do, both at the end: paste the instruction set and wire the placeholders, then run a quick completeness check.

Before starting, confirm two things with the deployer. First, that this workspace is on a plan that includes the full Notion AI custom-instructions feature (the ability to add your own AI instructions under Settings, then Notion AI); Clyo runs on that feature, so if their plan includes only a limited AI trial, the install step will not work reliably and they should upgrade first. Second, that they have the Open Clyo genome package on hand (the repo they cloned or downloaded); the instruction set you install at the end is the file `clyo-instructions.md` inside that package, so if they do not have it yet, have them get it before continuing. Ask them in plain words to reply "yes to both" once this workspace has the full Notion AI custom-instructions feature and they have the genome package on hand, and stop here and wait for that reply before creating anything.

Once they confirm, work through these eight surfaces in order. Run each one through the same three beats: check whether a surface with that name already exists (and if one does, stop and flag the collision as described above, since this deploy assumes a fresh empty workspace), create it while telling the user what you are making and why, then confirm it exists and is named correctly before moving to the next.

**Surface 1: Context database.** *(Check it does not already exist, create, then confirm before the next.)* Create a full-page database named "Context" with four properties: Name as the title property, Status as a select with options Active and Archived, Topic as a multi-select, and Detail as a text property. Tell the user that Detail is the field Clyo actually reads during retrieval: an entry without Detail is invisible to Clyo no matter how good its title is. Topic is a broad scoping tag, left open for the user to populate.

**Surface 2: Current Focus page.** *(Check it does not already exist, create, then confirm before the next.)* Create a plain page titled "Current Focus." Tell the user this is a short, living snapshot of their active priorities, not an archive, just what is true right now. The page starts blank, and they will fill it in after setup.

**Surface 3: Core Profile page.** *(Check it does not already exist, create, then confirm before the next.)* Create a plain page titled "Core Profile" and populate it with this starter structure. Create each section as a real page heading (not a code block), with the bracketed prompt text as plain text beneath each for the deployer to replace later:

- **How I think** (heading 3): [Your thinking style, how you process information, how you make decisions]
- **Strengths** (heading 3): [What you do well and want Clyo to lean into]
- **Growth edges** (heading 3): [Areas you are actively working on or want honest pushback on]
- **Values and motivations** (heading 3): [What matters most to you in work and life, what motivates and drives you]
- **Working preferences** (heading 3): [How you like to communicate, what pace feels right, any context Clyo should know about your setup]
- **Observations** (heading 4): [Catch-all for things that surface and don't fit neatly elsewhere]

Tell the user this page comes pre-populated with the six starter section headings already in place to guide them, and that after setup they fill in their own content beneath each heading. This is the page that lets Clyo respond in a way that fits them rather than a generic default, so it is worth filling in thoughtfully.

**Surface 4: Clyo Backlog page.** *(Check it does not already exist, create, then confirm before the next.)* Create a plain page titled "Clyo Backlog." Tell the user this is where ideas for improving Clyo itself get parked so nothing gets lost between sessions. The page starts empty, and they will fill it in after setup.

**Surface 5: Capture Queue page.** *(Check it does not already exist, create, then confirm before the next.)* Create a plain page titled "Capture Queue." Tell the user this holds pending items that surfaced in conversation and may be worth saving but have not been filed yet. The page starts empty, and they will fill it in after setup.

**Surface 6: Task Journal database.** *(Check it does not already exist, create, then confirm before the next.)* Create a full-page database named "Task Journal" with two properties: Name as the title property and Entry Date as a date property. The pattern is one entry per day, with completed work appended to that day's page body as a running point list. Clyo finds or creates today's entry by Entry Date and appends to it at session close. The daily entry template is optional, and Clyo works fully without it: at session close Clyo finds or creates that day's entry by Entry Date and appends the completed work, whether or not a template exists. So most deployers can skip the template entirely. Only mention the template option if the user says they want to use the Task Journal as a daily planning surface (a pre-loaded checklist, sections per kind of work). If they do, tell them this one piece is theirs to set up by hand: in the Task Journal database, open the template menu (the dropdown next to the blue New button), create a new template with the sections they want, and set it as the default, since setting a default database template is not a documented Notion-AI chat action; this deploy routes it to the manual database UI path rather than something you do for them through this chat. Do not create the template yourself or report that you have set a default template; if you try and cannot confirm it, you would be claiming a setup step that did not happen. Once they confirm they have either set up the template or chosen to skip it, continue.

**Surface 7: Drift Log database.** *(Check it does not already exist, create, then confirm before the next.)* Create a full-page database named "Drift Log" with seven properties: Name as the title property, Severity as a select with options Low, Medium, and High, Status as a select with options Open and Reviewed, Date as a date property, Model as a select with starter options Sonnet, Opus, GPT, and Gemini (the user can add others), Model version as a text property, and Notes as a text property. Tell the user this is where Clyo logs moments it drifted from its instructions so patterns can be caught and corrected. Note that Clyo records Model and Model version on every drift entry (it asks the user for the running model rather than guessing), which is why those are their own properties rather than folded into Notes.

**Surface 8: Open Clyo Version marker page.** *(Check it does not already exist, create, then confirm before the next.)* Create a plain page titled exactly "Open Clyo Version." Populate its body with a single line: "Open Clyo v0.1.0 installed [today's date]" with today's actual date filled in. This is a simple version stamp.

After all eight surfaces exist, pause and checkpoint with the user before the manual phase: restate that all eight surfaces are now built and named correctly (name them back), and verify the property count on the three databases: Context has four properties (Name, Status, Topic, and Detail), Task Journal has two (Name and Entry Date), and Drift Log has seven (Name, Severity, Status, Date, Model, Model version, and Notes). Also verify that the Core Profile page contains all six starter headings (How I think, Strengths, Growth edges, Values and motivations, Working preferences, and Observations). If the deployer identifies any missing properties, incorrect option sets, or missing Core Profile headings, fix them before moving to the install phase. Then restate that two main steps remain plus a final check, all theirs to do: pasting the instruction set, wiring the placeholders, then running the completeness check. Once they confirm the eight surfaces are correct, move to the install.

**Installing the instruction set.** Do these one at a time. After each step, stop and wait for the deployer's explicit reply before starting the next; do not run them together, and do not tell them Clyo is deployed until the final check passes.

**Step 1: Paste the instruction set.** Ask them to open Settings, then Notion AI (you can also open Settings with Cmd+K and search 'Notion AI'). Under the Instructions section, click "Add instructions," then "Create your own." Notion will create a dedicated instructions page and open it for editing. Before pasting, delete any text Notion has pre-filled into the page. Ask them to copy the full contents of `clyo-instructions.md` from the genome package and paste everything into that page. Long instruction sets can be silently truncated on paste, and a set cut off partway through will fail in quiet, confusing ways, so have them scroll to the very bottom and confirm the file pasted completely. The final line of the instruction set is exactly:

Exemption (the only one): a standalone single-answer turn with no task in flight this thread may end plainly.

Have them confirm that this exact line is present at the very bottom of the page. Then stop and wait: ask them to reply "DONE" once they have pasted the full file and confirmed that closing line is there. Do not continue to Step 2 until they reply.

**Step 2: Wire the placeholders.** Once they reply DONE, walk them through wiring. Plain action first: have them open Notion's in-page find inside the Instructions page (Cmd+F on Mac, Ctrl+F on Windows) and search the literal `URL]`. For each match the search highlights, they navigate to that location in the text, select and delete the full placeholder text from its opening bracket through its closing bracket, then type `@` and choose the matching surface from the Notion autocomplete by name. Note that even if your in-page find shows a replace function, you cannot use it here: a text replacement inserts plain text, and what each placeholder needs is a live @-mention linked to the surface, which only the type-@-and-select action creates. So each wiring is a manual locate-select-delete-retype sequence, not a find-and-replace. Every wiring placeholder ends in `URL]`, so this search surfaces exactly the ones to wire. There are nine placeholders across seven surfaces (the Open Clyo Version marker is not referenced by any of them): the Context database, Current Focus, Core Profile, Clyo Backlog, Capture Queue, Task Journal, and Drift Log. Two surfaces appear twice, Current Focus and Core Profile, so replace every instance, not just the first: six placeholders sit together in the Key Resources list, and the other three live in prose elsewhere (the Pre-Response Current Focus reference, the End-of-Response profile signal check Core Profile reference, and the Drift Log surveillance reference, which is Drift Log's only occurrence). Each placeholder must be replaced with an @-mention, never simply deleted, since a deleted placeholder leaves an instruction pointing at nothing while still passing the final search.

Present this caution to the deployer as a callout, set off from the steps so it is impossible to miss: the instruction set also contains the string `[PLACEHOLDER:` in two places inside the Write-target readiness rules (the "Detection" rule and the "Session-start readiness check"). Those two are intentional and must be left exactly as they are, never wired or deleted, because Clyo relies on them to detect an unconfigured write target. They do not end in `URL]`, so the search above never surfaces them, which is why searching `URL]` rather than `[PLACEHOLDER` keeps them safe.

Then stop and wait: ask them to reply "WIRED" once they have replaced every `URL]` match with an @-mention, selecting the matching surface from the Notion autocomplete each time so it becomes a live page mention (a blue linked surface name), not plain text. Do not continue to Step 3 until they reply.

**Step 3: Run a final completeness check.** Once they reply WIRED, run a three-part completeness check before telling them Clyo is deployed. When it is time to confirm Clyo responds, have the deployer open a new Notion AI chat in this workspace (press Cmd+J, or click New chat in the Notion AI panel) and run their verification there. The first part is a surface-by-surface confirmation that each wiring is a live mention; the second and third are text searches that catch any leftover or malformed placeholder. Run all three, in order, and do not declare the deploy complete until all three pass. The reason for the first part: a search for leftover placeholder text cannot tell the difference between a placeholder that was correctly wired to a live surface and one that was simply deleted. Both leave no leftover text, but only the wired one points at a real surface. So the live-mention confirmation is what actually proves the wiring is complete; the searches are a cross-check against it.

**Part 1: live-mention confirmation (the load-bearing check).** This is the check that actually proves the wiring is complete, so do it carefully. There are nine wiring spots. For each one, the deployer confirms two things: that the spot shows a live mention (a blue, clickable surface name, not plain text and not an empty gap), and that the mention points to the correctly-named surface. Have the deployer click into each mention (or hover to preview it) and confirm it opens the correctly-named surface, using the structural marker noted for each as a light correctness check:
1. Current Focus, in the Pre-Response context-loading step: a live mention pointing to the Current Focus page created in this setup.
2. Current Focus, in the Key Resources list: opens that same Current Focus page.
3. Core Profile, in the Key Resources list: opens the Core Profile page created in this setup (the one with the six starter headings: How I think, Strengths, Growth edges, Values and motivations, Working preferences, Observations).
4. Core Profile, in the End-of-Response profile signal check: opens that same Core Profile page.
5. Context database, in the Key Resources list: opens the Context database created in this setup (four properties: Name, Status, Topic, Detail).
6. Clyo Backlog, in the Key Resources list: a live mention pointing to the Clyo Backlog page created in this setup.
7. Capture Queue, in the Key Resources list: a live mention pointing to the Capture Queue page created in this setup.
8. Task Journal, in the Key Resources list: opens the Task Journal database created in this setup (two properties: Name, Entry Date).
9. Drift Log, in the Drift Log surveillance rule: opens the Drift Log database created in this setup (seven properties, including Model and Model version).
If any spot shows plain text, an empty gap, a mention of the wrong surface, or a mention that opens a different page than the one named for that spot, that spot is not correctly wired: send them back to Step 2 to fix it (delete the wrong mention, re-type @ and select the correct surface), then re-run this confirmation. Do not proceed until all nine are confirmed live and pointing to the right surface.

**Part 2: leftover-placeholder cross-check.** Before running this search, have them scroll the Instructions page once from top to bottom so all of its content is loaded. Then, using Notion's in-page find (Cmd+F on Mac, Ctrl+F on Windows), have them search the literal text `URL]` and confirm the result is zero. Every wiring placeholder ends in `URL]`, so zero means no wiring placeholder remains as unwired text. If any `URL]` matches appear, those spots still hold placeholder text; wire them in Step 2 and re-run Parts 1 and 2.

**Part 3: protected-reference cross-check.** Have them search the literal text `[PLACEHOLDER:` and confirm the result is exactly 2. These two are the intentional readiness references inside the Write-target readiness rules, which must never be wired or deleted. Exactly 2 confirms both are intact. If the result is fewer than 2, an intentional reference was deleted by mistake and the instruction set should be re-pasted from the genome package (return to Step 1) to restore it. If the result is greater than 2, the most likely cause is that wiring is incomplete, not that there are stray placeholders to remove: every un-wired wiring placeholder still reads `[PLACEHOLDER:` ... `URL]`, so it is counted here too, and a count near 11 (the nine wiring placeholders plus the two intentional references) means none of the placeholders have been wired yet. In that case route the deployer back to Step 2 to complete the wiring, then re-run Parts 1, 2, and 3. Only if Part 2's `URL]` search returns zero (so all nine wiring placeholders are gone) and `[PLACEHOLDER:` still exceeds 2 should they search for each occurrence of `[PLACEHOLDER:` on the page and compare each against the two known intentional references inside the Write-target readiness rules; any occurrence outside those two is then unexpected, so delete it if its origin is clear, or return to Step 1 and re-paste the full instruction set from the genome package to restore a clean baseline, then re-run the wiring and all three completeness checks.

Ask them to reply with the outcome of all three: all nine live mentions confirmed, the `URL]` count, and the `[PLACEHOLDER:` count. Do not tell them Clyo is deployed until all nine mentions are confirmed live and correct, `URL]` returns zero, and `[PLACEHOLDER:` returns exactly 2.

Once all three pass, Clyo is deployed, and you can tell them so warmly: all eight surfaces exist, the full instruction set is installed, and every reference resolves to a live surface. Then hand them into their first session. Tell them to open a new Notion AI chat in this workspace (Clyo is now running there) and, to start, simply say hello and tell Clyo what they are working on right now, or ask it to help them think through something. Let them know there is no special command to learn; Clyo reads its surfaces on its own. The one thing worth doing soon, whenever they are ready, is opening the Core Profile page and filling in those starter sections: the more context they add, the more Clyo will feel like it knows them from the start.
```
